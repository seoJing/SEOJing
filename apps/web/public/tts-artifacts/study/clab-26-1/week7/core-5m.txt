7주차 학습 자료 입문 강의 (처음이라면): 얄코 JS 입문 강의 Section 5 후반 Section 6 (생성자 함수·클래스·상속, 주요 빌트인 객체) 챕터 — 4:31:24부터 강의 스킵 가능: 아래 스킵 진단 문제 10개를 모두 맞히면 강의 시청 스킵 가능합니다. 심화 자료 (딥다이브 대비): 딥다이브 요약 GitHub 안녕하세요. 7주차입니다. 이번 주는 다른 언어 경험자에게 JS가 가장 낯설게 느껴지는 구간입니다. 프로토타입 은 Java나 Python의 클래스 상속과 근본적으로 다른 JS만의 상속 방식이고, 불변성 은 const를 써도 왜 완전히 불변이 아닌지를 이해하는 개념입니다. 타입 체크는 동적 타입 언어에서 런타임 안전성을 확보하는 방법입니다. 이번 주 비대면 자료는 대면 7주차의 프로젝트 구조와 책임 분리 주제와 연결됩니다. 객체를 직접 바꾸면 왜 예측이 어려워지는지, 타입을 확인하지 않으면 왜 API 응답 처리에서 문제가 생기는지, JS 객체가 어떤 방식으로 동작하는지 알아야 프로젝트가 커졌을 때 코드를 더 안전하게 나눌 수 있습니다. --- 스킵 진단 문제 아래 10문제를 막힘없이 풀 수 있다면, " " 이번 주 학습 자료를 생략하거나, 모르는 부분만 선택적으로 읽으셔도 좋습니다. " " 바로 하단의 더 알면 좋을 것들을 확인해보세요. --- 1. 불변성(Immutability) — const는 불변이 아니다 많은 분들이 const를 쓰면 값이 변하지 않는다 고 오해합니다. 하지만 const는 변수가 가리키는 참조(주소)를 고정 할 뿐, 그 주소에 있는 객체 내부 변경은 막지 않습니다. 얕은 복사 vs 깊은 복사 객체를 복사할 때도 주의가 필요합니다. spread(...)나 Object.assign()은 " " 얕은 복사 (shallow copy) 로, 1단계 깊이만 새로운 값으로 복사됩니다. 중첩 객체는 여전히 참조를 공유합니다. Object.freeze() — 진짜 불변 만들기 실무에서는 상태를 직접 변경하지 않고 새 객체를 만들어 반환하는 " " 불변 패턴 을 많이 사용합니다. React의 상태 관리(setState, useState)가 이 방식을 따릅니다. 지금은 React를 배우지 않은 상태이니 "나중에 React 배울 때 이 감각이 살아난다" 정도로만 넘겨두세요. 이유는 week10에서 자연스럽게 이어집니다. --- 2. 프로토타입 — JS의 상속 방식 Java나 Python은 클래스 기반 상속 입니다. 클래스를 정의하고 extends로 상속합니다. JS는 다릅니다. 프로토타입 기반 상속 으로, 객체가 다른 객체를 직접 상속합니다. ES6의 class 문법도 내부적으로는 프로토타입을 사용합니다. 프로토타입 체인 모든 JS 객체는 [[Prototype]]이라는 내부 슬롯을 가지며, 이 슬롯이 상위 프로토타입 객체를 가리킵니다. 프로퍼티를 참조할 때 자신에게 없으면 " " 프로토타입 체인을 따라 상위로 탐색 합니다. 생성자 함수와 prototype [[Prototype]] vs prototype — 헷갈리는 두 개념 이름이 비슷해서 매우 헷갈리는 두 개념입니다. 한 줄로 먼저 구분하면: " " [[Prototype]] 은 모든 객체가 자동으로 가지는 "보이지 않는 링크"(위로 찾아 올라갈 부모를 가리킴)이고, prototype 은 함수 객체에만 달린 프로퍼티 (그 함수로 new 해서 만들어질 인스턴스들의 부모가 될 객체) 입니다. [[Prototype]] : 모든 객체가 가지는 내부 슬롯. 상위 프로토타입을 가리킴. proto 로 접근 가능 (권장 안 함) prototype : 함수 객체만 가지는 프로퍼티. new로 인스턴스를 만들 때 인스턴스의 [[Prototype]]이 됨 원시값에서 메서드가 동작하는 이유 --- 3. 타입 체크 — typeof의 한계와 대안 정적 타입 언어(Java, TypeScript)에서는 컴파일 타임에 타입 오류를 잡아줍니다. 순수 JS는 런타임에야 타입이 결정되므로, 직접 확인이 필요한 경우가 있습니다. typeof의 한계 정확한 타입 확인: Object.prototype.toString.call() 이 API는 실무에서 직접 호출하는 일은 많지 않습니다 . 다만 "가장 정밀한 타입 체크의 원리"를 한 번 이해해두면, 라이브러리 내부에서 이 패턴이 나올 때 읽을 수 있습니다. 지금은 "이런 방법이 있다" " " 정도만 기억해도 충분해요. 실무에서 자주 쓰는 타입 체크 패턴 TypeScript를 사용하면 이런 런타임 타입 체크의 대부분을 컴파일 타임으로 옮길 수 있습니다. React 프로젝트에서 TypeScript가 사실상 표준인 이유 중 하나입니다. (TypeScript는 지금 주차에서는 다루지 않습니다. "이름만 들어두세요" 수준으로 OK.) --- 📝 학습 이해 퀴즈 아래 코드를 브라우저 콘솔에서 직접 실행 해보고 결과를 입력해보세요. --- 📝 7주차 스터디 인증 미션 아래 질문 중 하나를 직접 찾아보고 답을 댓글로 남겨주세요.
