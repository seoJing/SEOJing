Entity를 그대로 API 응답으로 노출하지 않는가? Request DTO 필드 이름이 프론트 요청 JSON과 맞는가? Response DTO 필드 이름이 프론트 기대값과 맞는가? 민감 정보가 응답에 포함되지 않는가? 이 체크리스트는 아직 낮은 단계입니다. 하지만 실제 협업에서는 이 낮은 단계의 실수가 정말 자주 납니다. 특히 AI가 빠르게 코드를 만들 때는 구조가 그럴듯해 보여도 DTO 이름 하나, API path 하나, 예외 응답 하나가 틀어져서 프론트 연결이 깨질 수 있습니다. ---
