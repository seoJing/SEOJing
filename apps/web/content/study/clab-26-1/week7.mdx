---
title: "프론트엔드 스터디 7주차: 불변성, 프로토타입, 타입 체크"
date: 2026-05-11
tags:
  - 프론트엔드
  - 스터디
  - JavaScript
  - 프로토타입
  - 불변성
  - 타입체크
description: JS에서 가장 낯선 개념 중 하나인 프로토타입 기반 상속을 다룹니다. const가 왜 불변을 보장하지 않는지, 타입을 런타임에 정확히 확인하는 방법까지 함께 배웁니다.
---

<Subtitle level={2}>7주차 학습 자료</Subtitle>

- <strong>학습 챕터:</strong> poiemaweb — 객체와 변경불가성 / 프로토타입 / 타입
  체크
- <strong>자료 바로가기:</strong>
  <Anchor external="https://poiemaweb.com/js-immutability">
    1. 객체와 변경불가성(Immutability)
  </Anchor>
  <Anchor external="https://poiemaweb.com/js-prototype">2. 프로토타입</Anchor>
  <Anchor external="https://poiemaweb.com/js-type-check">3. 타입 체크</Anchor>
- <strong>딥다이브 요약본 (보충용):</strong>
  <Anchor external="https://github.com/youngminss/Docs-modernJS__deepDive/tree/master/16-Prototype">
    딥다이브 요약 GitHub
  </Anchor>

<Paragraph>
  안녕하세요. 7주차입니다. 이번 주는 다른 언어 경험자에게 JS가 가장 낯설게
  느껴지는 구간입니다. <strong>프로토타입</strong>은 Java나 Python의 클래스
  상속과 근본적으로 다른 JS만의 상속 방식이고, <strong>불변성</strong>은
  `const`를 써도 왜 완전히 불변이 아닌지를 이해하는 개념입니다. 타입 체크는 동적
  타입 언어에서 런타임 안전성을 확보하는 방법입니다.
</Paragraph>

---

<Subtitle level={2}>스킵 진단 문제</Subtitle>

<Paragraph>
  아래 10문제를 막힘없이 풀 수 있다면,{" "}
  <strong>
    이번 주 학습 자료를 생략하거나, 모르는 부분만 선택적으로 읽으셔도 좋습니다.
  </strong>{" "}
  바로 하단의 더 알면 좋을 것들을 확인해보세요.
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="multiple"
    question="const로 선언한 객체에서 할 수 없는 것은 무엇일까요?"
    choices={[
      "프로퍼티 값 변경 (obj.x = 2)",
      "프로퍼티 추가 (obj.y = 3)",
      "변수가 다른 객체를 가리키도록 재할당 (obj = {})",
      "프로퍼티 삭제 (delete obj.x)",
    ]}
    answer={2}
    explanation="const는 변수 바인딩(참조)을 고정할 뿐, 객체 내부 변경은 막지 않습니다. 프로퍼티 변경/추가/삭제는 모두 가능합니다. 재할당(obj = {})만 TypeError가 발생합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="Object.freeze()의 한계는 무엇인가요?"
    choices={[
      "freeze된 객체는 읽기도 불가능하다",
      "최상위 프로퍼티만 동결되고, 중첩된 객체는 여전히 변경 가능하다",
      "freeze는 배열에는 적용되지 않는다",
      "freeze된 객체는 JSON으로 변환할 수 없다",
    ]}
    answer={1}
    explanation="Object.freeze()는 얕은 동결(shallow freeze)만 수행합니다. obj.nested.x = 1처럼 중첩 객체의 프로퍼티는 여전히 변경 가능합니다. 완전한 불변을 원한다면 deepFreeze 같은 재귀적 동결이 필요합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="JavaScript에서 모든 객체가 공통으로 갖는 메서드(toString, hasOwnProperty 등)는 어디서 오는 걸까요?"
    choices={[
      "JS 엔진이 각 객체에 자동으로 복사해준다",
      "Object.prototype에서 프로토타입 체인을 통해 상속받는다",
      "모든 객체 선언 시 자동으로 내장된다",
      "import 없이 사용할 수 있는 전역 함수다",
    ]}
    answer={1}
    explanation="모든 객체의 프로토타입 체인 최상단에는 Object.prototype이 있습니다. toString(), hasOwnProperty() 등은 Object.prototype에 정의되어 있고, 프로토타입 체인을 통해 모든 객체에서 접근 가능합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="typeof []의 결과는 무엇일까요?"
    choices={["'array'", "'object'", "'undefined'", "'list'"]}
    answer={1}
    explanation="배열도 객체이므로 typeof []는 'object'를 반환합니다. 배열 여부를 확인하려면 Array.isArray()를 사용해야 합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="생성자 함수로 만든 인스턴스가 부모의 메서드를 사용할 수 있는 이유는 무엇인가요?"
    choices={[
      "생성 시 메서드가 인스턴스에 복사되기 때문",
      "prototype 프로퍼티에 연결된 프로토타입 체인을 통해 탐색하기 때문",
      "JS 엔진이 자동으로 메서드를 주입하기 때문",
      "클래스 상속을 자동으로 처리하기 때문",
    ]}
    answer={1}
    explanation="인스턴스에서 프로퍼티를 찾을 때 자신의 프로퍼티에 없으면 [[Prototype]]을 따라 상위 프로토타입을 탐색합니다. 생성자 함수의 prototype 프로퍼티에 메서드를 추가하면 모든 인스턴스가 공유해서 사용할 수 있습니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="원시값인 문자열에서 'hello'.toUpperCase()처럼 메서드를 호출할 수 있는 이유는 무엇인가요?"
    choices={[
      "문자열은 사실 객체이기 때문",
      "JS 엔진이 일시적으로 String 래퍼 객체로 변환한 후 메서드를 실행하기 때문",
      "toUpperCase가 전역 함수이기 때문",
      "prototype에 직접 정의되어 있기 때문",
    ]}
    answer={1}
    explanation="원시값은 객체가 아니지만, 메서드 호출 시 JS 엔진이 자동으로 해당 래퍼 객체(String, Number, Boolean)로 임시 변환합니다. 메서드 실행 후 래퍼 객체는 즉시 사라집니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="instanceof 연산자는 무엇을 확인하나요?"
    choices={[
      "변수의 typeof 결과가 특정 타입인지 확인",
      "객체가 특정 생성자 함수의 prototype을 프로토타입 체인에 가지고 있는지 확인",
      "두 객체가 동일한 참조인지 확인",
      "객체가 특정 프로퍼티를 가지고 있는지 확인",
    ]}
    answer={1}
    explanation="instanceof는 오른쪽 피연산자의 prototype이 왼쪽 피연산자의 프로토타입 체인 어딘가에 있는지 확인합니다. [] instanceof Array는 true, [] instanceof Object도 true입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="Object.assign(target, source)의 동작으로 옳은 것은 무엇인가요?"
    choices={[
      "target과 source를 병합한 새 객체를 반환한다",
      "source의 프로퍼티를 target에 복사하고 target을 반환한다",
      "두 객체를 깊은 복사(deep copy)하여 병합한다",
      "source를 freeze한 후 target에 할당한다",
    ]}
    answer={1}
    explanation="Object.assign()은 source의 열거 가능한 프로퍼티를 target에 복사하고 target 자체를 반환합니다. 얕은 복사(shallow copy)로, 중첩 객체는 참조가 복사됩니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="null 여부를 정확히 확인하는 방법으로 올바른 것은 무엇인가요?"
    choices={[
      "typeof value === 'null'",
      "value instanceof null",
      "value === null",
      "Object.prototype.toString.call(value) === 'null'",
    ]}
    answer={2}
    explanation="typeof null은 'object'를 반환하므로 typeof로는 null을 확인할 수 없습니다. null 확인에는 항등 연산자(===)를 사용해야 합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="가장 정확하게 값의 타입을 확인할 수 있는 방법은 무엇인가요?"
    choices={[
      "typeof",
      "instanceof",
      "Object.prototype.toString.call()",
      "value.constructor.name",
    ]}
    answer={2}
    explanation="Object.prototype.toString.call(value)는 '[object Array]', '[object Null]', '[object Date]' 등 정확한 타입 문자열을 반환합니다. typeof는 null/배열을 'object'로, instanceof는 원시값에 동작하지 않는 한계가 있습니다."
  />
</ArticleQuiz>

---

<Subtitle level={2}>1. 불변성(Immutability) — const는 불변이 아니다</Subtitle>

<Paragraph>
  많은 분들이 <strong>`const`를 쓰면 값이 변하지 않는다</strong>고 오해합니다.
  하지만 const는 변수가 가리키는 <strong>참조(주소)를 고정</strong>할 뿐, 그
  주소에 있는 객체 내부 변경은 막지 않습니다.
</Paragraph>

```js
const obj = { x: 1 };
obj.x = 99; // 가능 — 내부 프로퍼티 변경
obj.y = 100; // 가능 — 프로퍼티 추가
delete obj.x; // 가능 — 프로퍼티 삭제

obj = {}; // TypeError — 재할당만 불가
```

<Subtitle level={3}>얕은 복사 vs 깊은 복사</Subtitle>

<Paragraph>
  객체를 복사할 때도 주의가 필요합니다. spread(`...`)나 `Object.assign()`은{" "}
  <strong>얕은 복사 (shallow copy)</strong>로, 1단계 깊이만 새로운 값으로
  복사됩니다. 중첩 객체는 여전히 참조를 공유합니다.
</Paragraph>

```js
const original = { a: 1, nested: { b: 2 } };

// 얕은 복사
const shallow = { ...original };
shallow.a = 99; // original.a 영향 없음
shallow.nested.b = 99; // original.nested.b도 99로 바뀜!

// 깊은 복사 (간단한 방법 — 함수/undefined 등은 사라짐)
const deep = JSON.parse(JSON.stringify(original));
deep.nested.b = 999; // original.nested.b 영향 없음
```

<Subtitle level={3}>Object.freeze() — 진짜 불변 만들기</Subtitle>

```js
const obj = Object.freeze({ x: 1, nested: { y: 2 } });

obj.x = 99; // 무시됨 (strict mode에서는 TypeError)
obj.nested.y = 99; // 가능! — freeze는 최상위만 동결

// 완전한 불변이 필요하면 재귀적으로 freeze해야 함
function deepFreeze(obj) {
  Object.keys(obj).forEach((key) => {
    if (typeof obj[key] === "object" && obj[key] !== null) {
      deepFreeze(obj[key]);
    }
  });
  return Object.freeze(obj);
}
```

<Paragraph>
  실무에서는 상태를 직접 변경하지 않고 새 객체를 만들어 반환하는{" "}
  <strong>불변 패턴</strong>을 많이 사용합니다. React의 상태 관리(`setState`,
  `useState`)가 이 방식을 따릅니다.
</Paragraph>

---

<Subtitle level={2}>2. 프로토타입 — JS의 상속 방식</Subtitle>

<Paragraph>
  Java나 Python은 <strong>클래스 기반 상속</strong>입니다. 클래스를 정의하고
  extends로 상속합니다. JS는 다릅니다. <strong>프로토타입 기반 상속</strong>
  으로, 객체가 다른 객체를 직접 상속합니다. ES6의 `class` 문법도 내부적으로는
  프로토타입을 사용합니다.
</Paragraph>

<Subtitle level={3}>프로토타입 체인</Subtitle>

<Paragraph>
  모든 JS 객체는 `[[Prototype]]`이라는 내부 슬롯을 가지며, 이 슬롯이 상위
  프로토타입 객체를 가리킵니다. 프로퍼티를 참조할 때 자신에게 없으면{" "}
  <strong>프로토타입 체인을 따라 상위로 탐색</strong>합니다.
</Paragraph>

```js
const obj = { x: 1 };

// obj에는 toString이 없지만 사용 가능
console.log(obj.toString()); // "[object Object]"

// 프로토타입 체인: obj → Object.prototype → null
// toString은 Object.prototype에 있음
```

<Subtitle level={3}>생성자 함수와 prototype</Subtitle>

```js
function Person(name) {
  this.name = name;
  // 여기에 메서드를 넣으면 인스턴스마다 함수가 복사됨 — 비효율
}

// prototype에 메서드를 추가하면 모든 인스턴스가 공유
Person.prototype.greet = function () {
  console.log(`안녕하세요, ${this.name}입니다.`);
};

const p1 = new Person("철수");
const p2 = new Person("영희");

p1.greet(); // "안녕하세요, 철수입니다."
p2.greet(); // "안녕하세요, 영희입니다."

// p1과 p2의 greet는 같은 함수를 공유
console.log(p1.greet === p2.greet); // true
```

<Subtitle level={3}>[[Prototype]] vs prototype — 헷갈리는 두 개념</Subtitle>

- <strong>`[[Prototype]]`</strong>: 모든 객체가 가지는 내부 슬롯. 상위
  프로토타입을 가리킴. `__proto__`로 접근 가능 (권장 안 함)
- <strong>`prototype`</strong>: 함수 객체만 가지는 프로퍼티. `new`로 인스턴스를
  만들 때 인스턴스의 `[[Prototype]]`이 됨

```js
function Foo() {}

const foo = new Foo();

console.log(foo.__proto__ === Foo.prototype); // true
console.log(Foo.prototype.constructor === Foo); // true
```

<Subtitle level={3}>원시값에서 메서드가 동작하는 이유</Subtitle>

```js
"hello".toUpperCase(); // "HELLO"
(42).toFixed(2); // "42.00"

// 원시값은 객체가 아닌데 어떻게 메서드가 있을까?
// JS 엔진이 메서드 호출 시 임시로 래퍼 객체(String, Number)로 변환
// 메서드 실행 후 래퍼 객체는 즉시 제거됨
```

---

<Subtitle level={2}>3. 타입 체크 — typeof의 한계와 대안</Subtitle>

<Paragraph>
  정적 타입 언어(Java, TypeScript)에서는 컴파일 타임에 타입 오류를 잡아줍니다.
  순수 JS는 런타임에야 타입이 결정되므로, 직접 확인이 필요한 경우가 있습니다.
</Paragraph>

<Subtitle level={3}>typeof의 한계</Subtitle>

```js
typeof 1; // 'number'
typeof "hello"; // 'string'
typeof true; // 'boolean'
typeof undefined; // 'undefined'
typeof function () {}; // 'function'

// 한계: 아래는 모두 'object'를 반환
typeof null; // 'object' ← 버그
typeof []; // 'object'
typeof {}; // 'object'
typeof new Date(); // 'object'
```

<Subtitle level={3}>
  정확한 타입 확인: Object.prototype.toString.call()
</Subtitle>

```js
function getType(value) {
  return Object.prototype.toString.call(value).slice(8, -1);
}

getType(1); // 'Number'
getType("hello"); // 'String'
getType(null); // 'Null'
getType([]); // 'Array'
getType({}); // 'Object'
getType(new Date()); // 'Date'
getType(undefined); // 'Undefined'
```

<Subtitle level={3}>실무에서 자주 쓰는 타입 체크 패턴</Subtitle>

```js
// 배열 확인 — Array.isArray() 권장
Array.isArray([]);        // true
Array.isArray({});        // false

// null 확인 — === 사용
value === null

// null 또는 undefined 확인
value == null             // null과 undefined 모두 해당 (예외적으로 == 허용)

// 특정 인스턴스 확인 — instanceof
new Date() instanceof Date  // true
[] instanceof Array          // true
[] instanceof Object         // true (체인 전체 탐색)
```

<Paragraph>
  TypeScript를 사용하면 이런 런타임 타입 체크의 대부분을 컴파일 타임으로 옮길 수
  있습니다. React 프로젝트에서 TypeScript가 사실상 표준인 이유 중 하나입니다.
</Paragraph>

---

<Subtitle level={2}>📝 학습 이해 퀴즈</Subtitle>

<Paragraph>
  아래 코드를 <strong>브라우저 콘솔에서 직접 실행</strong>해보고 결과를
  입력해보세요.
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="description"
    question="아래 코드에서 마지막 console.log의 출력 결과는?"
    code={`const obj = Object.freeze({ x: 1, nested: { y: 2 } });
obj.x = 99;
obj.nested.y = 99;
console.log(obj.x, obj.nested.y);`}
    answer="1, 99"
    explanation="Object.freeze()는 최상위 프로퍼티만 동결합니다. obj.x 변경은 무시되어 1 그대로지만, 중첩 객체 obj.nested는 freeze되지 않아 obj.nested.y는 99로 변경됩니다."
  />
  <ArticleQuizItem
    mode="description"
    question="아래 코드에서 두 console.log의 출력 결과를 순서대로 입력하세요."
    code={`function Animal(name) {
  this.name = name;
}
Animal.prototype.speak = function () {
  return this.name + " 소리를 냅니다.";
};

const a = new Animal("고양이");
console.log(a.speak());
console.log(a.hasOwnProperty("speak"));`}
answer="고양이 소리를 냅니다., false"
explanation="speak는 a 자신의 프로퍼티가 아니라 Animal.prototype에 있습니다. 프로토타입 체인으로 접근할 수 있어 호출은 가능하지만, hasOwnProperty는 자신의 프로퍼티만 true를 반환하므로 false입니다."
/>

  <ArticleQuizItem
    mode="description"
    question="아래 코드의 세 줄 출력 결과를 순서대로 입력하세요."
    code={`console.log(typeof null);
console.log(typeof []);
console.log(Array.isArray([]));`}
    answer="object, object, true"
    explanation="typeof null과 typeof []는 모두 'object'를 반환합니다. 배열을 정확히 확인하려면 Array.isArray()를 사용해야 하며, 이 경우 true를 반환합니다."
  />
</ArticleQuiz>

---

<Subtitle level={2}>📝 7주차 스터디 인증 미션</Subtitle>

<Paragraph>
  다가오는 스터디 모임 전, 아래 질문에 대해 본인만의 말로 설명한 뒤 댓글로
  남겨주세요.
</Paragraph>

> **Q1. JavaScript에서 프로토타입 기반 상속이란 무엇인가요? Java의 클래스 기반 상속과 어떻게 다른지 설명해주세요.**
>
> <br />
>
> **Q2. `var`, `let`, `const`의 차이를 설명해주세요. (이번 주 학습 내용과 연결해서, `const`가 왜 완전한 불변을 보장하지 않는지도 포함해주세요.)**
