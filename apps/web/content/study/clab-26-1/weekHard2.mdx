---
title: "프론트엔드 스터디 심화: 에러 처리와 정규 표현식"
date: 2026-06-08
tags:
  - 프론트엔드
  - 스터디
  - JavaScript
  - 에러처리
  - 정규표현식
description: 지금 당장 읽지 않아도 됩니다. React를 배우면서 에러 처리가 필요해지거나 면접 대비가 필요할 때 참고하세요.
---

<Subtitle level={2}>언제 이 내용이 필요한가?</Subtitle>

<Paragraph>
  이 자료는 <strong>지금 당장 읽지 않아도 됩니다.</strong> 1학기 React 스터디를
  마치고, 아래 상황이 되었을 때 돌아오세요.
</Paragraph>

- <strong>useEffect 안에서 fetch 에러 처리</strong>를 하고 싶은데 try/catch가
  낯설 때
- <strong>API 응답 유효성 검사, 폼 입력 검증</strong>에 정규 표현식을 쓰고 싶을
  때
- <strong>커스텀 에러 클래스</strong>로 에러 종류를 구별하고 싶을 때
- <strong>면접</strong>에서 에러 처리나 정규 표현식 관련 질문을 받게 될 때

---

<Subtitle level={2}>심화 학습 자료</Subtitle>

- <strong>학습 챕터:</strong> poiemaweb — 에러 처리 / 정규 표현식
- <strong>자료 바로가기:</strong>
  <Anchor external="https://poiemaweb.com/js-error">1. 에러 처리</Anchor>
  <Anchor external="https://poiemaweb.com/js-regexp">2. 정규 표현식</Anchor>
- <strong>딥다이브 요약본 (보충용):</strong>
  <Anchor external="https://github.com/youngminss/Docs-modernJS__deepDive/tree/master/47-Error">
    딥다이브 요약 GitHub
  </Anchor>

<Paragraph>
  <strong>에러 처리</strong>는 async/await와 짝을 이루는 개념으로, 프로덕션
  코드에서 예외 상황을 안전하게 처리하는 방법입니다.{" "}
  <strong>정규 표현식</strong>은 처음 보면 암호처럼 느껴지지만, 자주 쓰는 패턴
  몇 가지만 익혀두면 입력 유효성 검사, 문자열 파싱에서 강력하게 활용할 수
  있습니다.
</Paragraph>

---

<Subtitle level={2}>스킵 진단 문제</Subtitle>

<Paragraph>
  아래 10문제를 막힘없이 풀 수 있다면,{" "}
  <strong>
    이 자료 전체를 생략하거나 모르는 부분만 선택적으로 읽어도 됩니다.
  </strong>
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="multiple"
    question="try/catch/finally에 대한 설명으로 옳은 것은 무엇인가요?"
    choices={[
      "catch는 에러가 없어도 항상 실행된다",
      "finally는 에러 발생 여부와 관계없이 항상 실행된다",
      "try 블록에서 return을 하면 finally가 실행되지 않는다",
      "catch에서 에러를 다시 throw하면 finally가 실행되지 않는다",
    ]}
    answer={1}
    explanation="finally 블록은 try/catch 실행 후 에러 여부와 관계없이 항상 실행됩니다. try에서 return을 해도, catch에서 에러를 re-throw 해도 finally는 실행됩니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="Error 객체가 기본으로 가지는 프로퍼티가 아닌 것은 무엇인가요?"
    choices={["message", "name", "stack", "code"]}
    answer={3}
    explanation="Error 객체의 기본 프로퍼티는 message(에러 메시지), name(에러 타입 이름), stack(호출 스택)입니다. code는 기본 프로퍼티가 아니며, 특정 에러 타입(NodeJS 등)에서 추가하는 경우가 있습니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="커스텀 에러 클래스를 만들 때 올바른 방법은 무엇인가요?"
    choices={[
      "Error 객체를 직접 수정한다",
      "class CustomError extends Error로 Error를 상속한다",
      "throw에 문자열을 전달한다",
      "window.onerror를 재정의한다",
    ]}
    answer={1}
    explanation="커스텀 에러는 Error 클래스를 상속하여 만듭니다. constructor에서 super(message)를 호출하고, this.name을 설정하면 instanceof로 에러 종류를 구별할 수 있습니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="정규 표현식 /^hello/에서 ^의 의미는 무엇인가요?"
    choices={[
      "hello를 부정한다",
      "문자열의 시작을 의미한다",
      "대소문자를 무시한다",
      "hello를 0번 이상 반복한다",
    ]}
    answer={1}
    explanation="^는 문자열의 시작을 의미하는 앵커입니다. /^hello/는 문자열이 'hello'로 시작하는지를 검사합니다. $는 문자열의 끝을 의미합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="정규 표현식 플래그 g와 i의 의미는 무엇인가요?"
    choices={[
      "g: 전역 검색 (모든 매칭 반환), i: 대소문자 구분 없이 검색",
      "g: 탐욕적 매칭, i: 비탐욕적 매칭",
      "g: 줄바꿈 포함 검색, i: 인덱스 반환",
      "g: 그룹 캡처, i: 인라인 검색",
    ]}
    answer={0}
    explanation="g(global) 플래그는 매칭을 첫 번째에서 멈추지 않고 모든 매칭을 찾습니다. i(insensitive) 플래그는 대소문자를 구분하지 않고 검색합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="str.replace(/a/g, 'b')와 str.replaceAll('a', 'b')의 동작은 어떻게 다른가요?"
    choices={[
      "동작이 완전히 다르다",
      "동작은 같고 모든 'a'를 'b'로 교체하지만, replaceAll은 ES2021+에서 사용 가능하다",
      "replace는 첫 번째만, replaceAll은 전부 바꾼다",
      "replace는 문자열에만, replaceAll은 배열에도 사용 가능하다",
    ]}
    answer={1}
    explanation="/a/g와 replaceAll('a', 'b')는 모든 'a'를 'b'로 교체하는 동일한 동작을 합니다. replaceAll은 ES2021에 도입된 더 직관적인 대안입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="async 함수 내부에서 에러가 발생하면 어떻게 처리하나요?"
    choices={[
      "에러는 자동으로 무시된다",
      "try/catch 블록으로 처리하거나, 반환된 Promise에 .catch()를 사용한다",
      "process.exit()가 자동으로 호출된다",
      "에러는 항상 전역으로 전파된다",
    ]}
    answer={1}
    explanation="async 함수에서 에러가 발생하면 반환된 Promise가 rejected 상태가 됩니다. 내부에서 try/catch로 처리하거나, 함수 호출부에서 .catch()로 처리할 수 있습니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="정규 표현식 \\d, \\w, \\s의 의미로 옳은 것은 무엇인가요?"
    choices={[
      "\\d: 문자, \\w: 공백, \\s: 숫자",
      "\\d: 숫자(0-9), \\w: 단어 문자(알파벳·숫자·_), \\s: 공백 문자",
      "\\d: 대문자, \\w: 소문자, \\s: 특수문자",
      "\\d: 점(.), \\w: 별표(*), \\s: 슬래시(/)",
    ]}
    answer={1}
    explanation="\\d는 [0-9]와 동일한 숫자, \\w는 [a-zA-Z0-9_]와 동일한 단어 문자, \\s는 공백·탭·줄바꿈을 포함하는 공백 문자입니다. 대문자 \\D, \\W, \\S는 각각의 부정입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="에러를 catch한 후 다시 throw하는 이유는 무엇인가요?"
    choices={[
      "에러를 숨기기 위해",
      "현재 레이어에서 처리할 수 없는 에러를 상위 레이어로 전파하기 위해",
      "에러를 복제하기 위해",
      "finally 블록을 실행시키기 위해",
    ]}
    answer={1}
    explanation="에러를 로깅하거나 일부 처리 후, 현재 함수에서 완전히 처리할 수 없을 때 re-throw합니다. 예를 들어 네트워크 에러를 잡아 로깅한 뒤, 상위 컴포넌트에서 에러 UI를 보여주도록 전파하는 패턴입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="정규 표현식에서 캡처 그룹 ()의 역할은 무엇인가요?"
    choices={[
      "선택적 매칭을 나타낸다",
      "매칭된 부분을 그룹으로 묶어 별도로 추출할 수 있게 한다",
      "0번 이상 반복을 나타낸다",
      "문자 집합을 정의한다",
    ]}
    answer={1}
    explanation="()는 캡처 그룹을 만들어 매칭된 부분을 별도로 추출할 수 있습니다. '2026-06-08'.match(/(\d{4})-(\d{2})-(\d{2})/)는 [전체, '2026', '06', '08']을 반환합니다."
  />
</ArticleQuiz>

---

<Subtitle level={2}>1. 에러 처리 — 안전한 코드 만들기</Subtitle>

<Subtitle level={3}>try/catch/finally 기본</Subtitle>

```js
function divide(a, b) {
  if (b === 0) throw new Error("0으로 나눌 수 없습니다");
  return a / b;
}

try {
  const result = divide(10, 0);
  console.log(result);
} catch (err) {
  console.error(err.message); // "0으로 나눌 수 없습니다"
  console.error(err.name); // "Error"
} finally {
  console.log("항상 실행"); // 에러 여부 관계없이 실행
}
```

<Subtitle level={3}>내장 에러 타입</Subtitle>

```js
// JS에서 자주 마주치는 에러 타입들
try {
  null.property; // TypeError
  undeclaredVar; // ReferenceError
  JSON.parse("{bad}"); // SyntaxError
} catch (err) {
  if (err instanceof TypeError) {
    console.log("타입 에러:", err.message);
  } else if (err instanceof ReferenceError) {
    console.log("참조 에러:", err.message);
  }
}
```

<Subtitle level={3}>커스텀 에러 — Error 상속</Subtitle>

```js
class ValidationError extends Error {
  constructor(message, field) {
    super(message);
    this.name = "ValidationError";
    this.field = field;
  }
}

class NetworkError extends Error {
  constructor(message, statusCode) {
    super(message);
    this.name = "NetworkError";
    this.statusCode = statusCode;
  }
}

// 에러 종류에 따라 다르게 처리
try {
  throw new ValidationError("이메일 형식이 올바르지 않습니다", "email");
} catch (err) {
  if (err instanceof ValidationError) {
    console.log(`[${err.field}] ${err.message}`);
  } else if (err instanceof NetworkError) {
    console.log(`HTTP ${err.statusCode}: ${err.message}`);
  } else {
    throw err; // 처리할 수 없는 에러는 다시 전파
  }
}
```

<Subtitle level={3}>async/await 에러 처리</Subtitle>

```js
// 기본 패턴
async function fetchUser(id) {
  try {
    const res = await fetch(`/api/users/${id}`);
    if (!res.ok) {
      throw new NetworkError("요청 실패", res.status);
    }
    return await res.json();
  } catch (err) {
    console.error("fetchUser 에러:", err.message);
    throw err; // 필요하면 상위로 전파
  }
}

// 호출부 처리
async function loadUser() {
  try {
    const user = await fetchUser(1);
    setUser(user);
  } catch (err) {
    setError(err.message);
  }
}
```

---

<Subtitle level={2}>2. 정규 표현식 — 패턴으로 문자열 다루기</Subtitle>

<Paragraph>
  정규 표현식은 처음 보면 어렵지만, 자주 쓰는 패턴 몇 가지를 익혀두면 입력
  유효성 검사, 문자열 파싱, 포맷 변환 등에서 강력하게 활용할 수 있습니다.
</Paragraph>

<Subtitle level={3}>자주 쓰는 패턴</Subtitle>

```js
// 문자 클래스
const digits = /\d/; // 숫자 [0-9]
const words = /\w/; // 단어 문자 [a-zA-Z0-9_]
const spaces = /\s/; // 공백 문자
const any = /./; // 줄바꿈 제외 모든 문자

// 수량자
const zeroOrMore = /a*/; // 0번 이상
const oneOrMore = /a+/; // 1번 이상
const optional = /a?/; // 0번 또는 1번
const exactly3 = /a{3}/; // 정확히 3번
const twoToFour = /a{2,4}/; // 2~4번

// 앵커
const start = /^hello/; // hello로 시작
const end = /world$/; // world로 끝
const exact = /^hello world$/; // 정확히 이 문자열
```

<Subtitle level={3}>실무 패턴 예시</Subtitle>

```js
// 이메일 유효성 검사
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
emailRegex.test("user@example.com"); // true
emailRegex.test("invalid-email"); // false

// 한국 휴대폰 번호
const phoneRegex = /^01[016789]-?\d{3,4}-?\d{4}$/;
phoneRegex.test("010-1234-5678"); // true

// URL에서 도메인 추출
const url = "https://www.example.com/path?query=1";
const domain = url.match(/https?:\/\/([^\/]+)/)?.[1];
// "www.example.com"

// 날짜 포맷 변환 (YYYY-MM-DD → YYYY.MM.DD)
"2026-06-08".replace(/(\d{4})-(\d{2})-(\d{2})/, "$1.$2.$3");
// "2026.06.08"
```

<Subtitle level={3}>주요 메서드</Subtitle>

```js
const str = "Hello World hello";
const regex = /hello/gi; // g: 전역, i: 대소문자 무시

// test — 매칭 여부 (boolean)
regex.test(str); // true

// match — 매칭 결과 배열
str.match(regex); // ["Hello", "hello"]

// replace — 교체
str.replace(regex, "Hi"); // "Hi World Hi"

// split — 분리
"a,b,,c".split(/,+/); // ["a", "b", "c"]

// search — 첫 번째 매칭 인덱스
str.search(/World/); // 6
```

---

<Subtitle level={2}>📝 학습 이해 퀴즈</Subtitle>

<Paragraph>
  아래 코드를 <strong>브라우저 콘솔에서 직접 실행</strong>해보고 결과를
  입력해보세요.
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="essay"
    question="아래 코드의 출력 결과를 순서대로 입력하세요."
    code={`function riskyOperation() {
  throw new TypeError("타입 에러 발생");
}

try {
riskyOperation();
console.log("A");
} catch (err) {
console.log("B:", err.name);
} finally {
console.log("C");
}`}
answer="B: TypeError, C"
explanation="throw로 에러가 발생하면 try 블록의 나머지 코드('A')는 실행되지 않고 즉시 catch로 이동합니다. err.name은 'TypeError'입니다. finally는 항상 실행되어 'C'가 출력됩니다."
/>

  <ArticleQuizItem
    mode="essay"
    question="아래 코드의 출력 결과는 무엇인가요?"
    code={`const str = "2026-06-08";
const result = str.match(/(\d{4})-(\d{2})-(\d{2})/);
console.log(result[1]);`}
    answer="2026"
    explanation="정규 표현식의 캡처 그룹 ()은 매칭 결과 배열에서 인덱스 1부터 순서대로 담깁니다. result[0]은 전체 매칭 '2026-06-08', result[1]은 첫 번째 그룹 '2026'입니다."
  />
  <ArticleQuizItem
    mode="essay"
    question="아래 코드에서 emailRegex.test()의 결과를 순서대로 입력하세요."
    code={`const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

console.log(emailRegex.test("user@example.com"));
console.log(emailRegex.test("user@"));
console.log(emailRegex.test("user.name@domain.co.kr"));`}
answer="true, false, true"
explanation="첫 번째는 올바른 이메일 형식이라 true, 두 번째는 @ 이후에 도메인이 없어 false, 세 번째는 서브도메인 포함 올바른 형식이라 true입니다."
/>

</ArticleQuiz>

---

<Subtitle level={2}>📝 심화 인증 미션</Subtitle>

<Paragraph>
  아래 질문에 답해보세요. 직접 코드를 실행해보고 이유를 설명할 수 있어야 합니다.
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="essay"
    question="아래 코드에서 finally 블록은 실행되나요? 실행된다면 어떤 값이 출력되는지, 그 이유를 설명하세요."
    code={`function test() {
  try {
    throw new Error("에러 발생");
    console.log("A");
  } catch (err) {
    console.log("B:", err.message);
    return "catch에서 반환";
  } finally {
    console.log("C");
  }
}

console.log(test());`}
answer="B: 에러 발생, C, catch에서 반환 — finally는 try/catch의 return 이후에도 반드시 실행됩니다. 출력 순서는 B → C → (반환값 출력)"
explanation="finally는 에러 여부, return 여부와 관계없이 항상 실행됩니다. catch에서 return을 해도 finally가 먼저 실행된 뒤 실제 반환이 이루어집니다."
/>

  <ArticleQuizItem
    mode="essay"
    question="아래 코드에서 어떤 에러가 발생하는지 확인하고, 에러 종류별로 다르게 처리하도록 catch 블록을 수정해보세요."
    code={`async function fetchData(url) {
  const res = await fetch(url);
  const data = await res.json();
  return data;
}

fetchData("https://invalid-url-that-does-not-exist.com");`}
answer="네트워크 오류로 TypeError가 발생합니다. catch 블록에서 if (err instanceof TypeError) {/* 네트워크 에러 처리 */} else { throw err; }처럼 에러 타입을 instanceof로 구별해서 처리할 수 있습니다."
explanation="fetch가 네트워크 자체에 접근하지 못하면 TypeError: Failed to fetch가 발생합니다. HTTP 4xx/5xx 응답은 에러가 아니므로 res.ok 체크가 별도로 필요합니다. 에러 타입을 instanceof로 구별하면 ValidationError, NetworkError 등 각각에 맞는 처리가 가능합니다."
/>

  <ArticleQuizItem
    mode="essay"
    question="아래 이메일 정규 표현식을 이용해 유효한 이메일과 유효하지 않은 이메일을 각각 2개씩 만들어보세요. 어떤 패턴이 통과/실패하는지 규칙을 설명하세요."
    code={`const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;`}
    answer="유효: user@example.com, user.name+tag@domain.co.kr / 유효하지 않음: user@, @domain.com — 규칙: @ 앞에 공백/@ 없이 1자 이상, @ 뒤에 공백/@ 없이 1자 이상, 점(.) 뒤에 공백/@ 없이 1자 이상"
    explanation="[^\\s@]+는 공백과 @가 아닌 문자가 1개 이상이라는 뜻입니다. ^와 $로 전체 문자열을 검사합니다. 이 정규 표현식은 기본적인 형식만 검사하며, 실제 서버에서 이메일 존재 여부는 별도로 확인해야 합니다."
  />
</ArticleQuiz>

---

<Subtitle level={2}>관련 포스팅</Subtitle>

- <Anchor href="/blog/study/clab-26-1/week10">
    프론트엔드 스터디 심화: this, 실행 컨텍스트, 이터러블
  </Anchor>
