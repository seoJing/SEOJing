---
title: "프론트엔드 스터디 심화: this, 실행 컨텍스트, 이터러블"
date: 2026-06-01
tags:
  - 프론트엔드
  - 스터디
  - JavaScript
  - this
  - 실행컨텍스트
  - 이터러블
description: React를 배우면서 this가 헷갈리거나, 면접 대비가 필요할 때 참고하는 JS 심화 자료입니다. 이 내용을 지금 당장 완벽히 이해하지 않아도 React를 배우는 데 지장이 없습니다.
---

<Subtitle level={2}>심화 학습 자료</Subtitle>

- <strong>학습 챕터:</strong> poiemaweb — this / 실행 컨텍스트 / 이터러블
- <strong>자료 바로가기:</strong>
  <Anchor external="https://poiemaweb.com/js-this">1. this</Anchor>
  <Anchor external="https://poiemaweb.com/js-execution-context">
    2. 실행 컨텍스트
  </Anchor>
  <Anchor external="https://poiemaweb.com/es6-iteration-for-of">
    3. 이터러블과 for...of
  </Anchor>
- <strong>딥다이브 요약본 (보충용):</strong>
  <Anchor external="https://github.com/youngminss/Docs-modernJS__deepDive/tree/master/22-This">
    딥다이브 요약 GitHub
  </Anchor>

<Paragraph>
  이 자료는 1학기 커리큘럼에서 빠진 JS 심화 내용으로, React를 배우다가 `this`가
  헷갈리거나 면접 준비가 필요한 시점에 참고하는 자료입니다.{" "}
  <strong>지금 당장 읽지 않아도 됩니다.</strong> 이런 내용이 있다는 것만 알고
  넘어가도 좋습니다.
</Paragraph>

---

<Subtitle level={2}>스킵 진단 문제</Subtitle>

<Paragraph>
  이 자료를 지금 읽을 필요가 없다고 판단되면, 나중에 필요할 때 참고하세요. 아래
  문제를 막힘없이 풀 수 있다면 넘어가도 좋습니다.
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="multiple"
    question="일반 함수 호출에서 this는 무엇을 가리키나요? (strict mode 아님)"
    choices={[
      "함수 자신",
      "전역 객체 (브라우저에서는 window)",
      "undefined",
      "null",
    ]}
    answer={1}
    explanation="일반 함수로 호출할 때 this는 전역 객체를 가리킵니다. 브라우저에서는 window, Node.js에서는 global입니다. strict mode에서는 undefined입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="메서드로 호출할 때 this는 무엇을 가리키나요?"
    choices={[
      "항상 전역 객체",
      "메서드를 소유한 객체 (호출한 객체)",
      "메서드가 선언된 곳의 스코프",
      "항상 undefined",
    ]}
    answer={1}
    explanation="메서드 호출 시 this는 그 메서드를 호출한 객체를 가리킵니다. obj.method()에서 this는 obj입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="화살표 함수(arrow function)의 this 동작은 일반 함수와 어떻게 다른가요?"
    choices={[
      "화살표 함수는 this를 가지지 않으며, 선언된 위치의 상위 스코프 this를 그대로 사용한다",
      "화살표 함수에서 this는 항상 전역 객체다",
      "화살표 함수에서 this는 항상 undefined다",
      "화살표 함수에서 this는 함수 자신이다",
    ]}
    answer={0}
    explanation="화살표 함수는 자체 this를 가지지 않습니다. 선언 시점의 상위 스코프 this를 캡처하여 사용합니다(렉시컬 this). 그래서 콜백이나 이벤트 핸들러에서 this를 유지해야 할 때 유용합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="call, apply, bind의 공통점은 무엇인가요?"
    choices={[
      "함수를 즉시 실행하고 새 함수를 반환한다",
      "this를 명시적으로 지정할 수 있다",
      "비동기 함수에서만 사용 가능하다",
      "화살표 함수에도 적용된다",
    ]}
    answer={1}
    explanation="call, apply, bind 모두 this를 명시적으로 바인딩하는 메서드입니다. call과 apply는 함수를 즉시 호출하고(인자 방식 차이), bind는 this가 고정된 새 함수를 반환합니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="실행 컨텍스트(Execution Context)란 무엇인가요?"
    choices={[
      "JS 파일을 실행하는 Node.js 환경",
      "코드가 실행되는 환경으로, 변수·스코프·this 등의 정보를 담은 객체",
      "비동기 코드를 처리하는 특별한 공간",
      "함수가 선언된 위치",
    ]}
    answer={1}
    explanation="실행 컨텍스트는 코드가 실행될 때 생성되는 환경 정보 묶음입니다. 변수 환경(Variable Environment), 렉시컬 환경(Lexical Environment), this 바인딩 정보를 포함합니다. 콜 스택에 쌓이며 관리됩니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="이터러블(Iterable)이란 무엇인가요?"
    choices={[
      "배열과 동일한 자료구조",
      "Symbol.iterator 메서드를 구현한 객체로, for...of와 스프레드 등을 사용할 수 있는 것",
      "인덱스로 접근 가능한 모든 객체",
      "length 프로퍼티를 가진 객체",
    ]}
    answer={1}
    explanation="이터러블은 Symbol.iterator 메서드를 구현한 객체입니다. 배열, 문자열, Map, Set 등이 이터러블이며, for...of, 스프레드(...), 구조 분해 할당 등을 사용할 수 있습니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="Set 자료구조의 특징으로 옳은 것은 무엇인가요?"
    choices={[
      "키-값 쌍으로 저장하며 중복 키를 허용하지 않는다",
      "중복을 허용하지 않는 값의 집합이다",
      "삽입 순서를 보장하지 않는다",
      "인덱스로 접근할 수 있다",
    ]}
    answer={1}
    explanation="Set은 중복 없는 값의 집합입니다. 삽입 순서를 보장하고, 이터러블입니다. 중복 제거에 자주 활용됩니다: [...new Set([1, 2, 2, 3])] → [1, 2, 3]"
  />
  <ArticleQuizItem
    mode="multiple"
    question="Map과 일반 객체({})의 가장 큰 차이점은 무엇인가요?"
    choices={[
      "Map은 문자열 키만 사용할 수 있다",
      "Map은 어떤 타입이든 키로 사용할 수 있고, 삽입 순서를 보장하며, size로 크기를 알 수 있다",
      "Map은 이터러블이 아니다",
      "Map은 프로토타입 체인이 없다",
    ]}
    answer={1}
    explanation="일반 객체는 문자열/심볼만 키로 사용 가능하지만, Map은 객체, 함수, 원시값 등 모든 타입을 키로 사용할 수 있습니다. 삽입 순서를 보장하고, map.size로 크기를 확인할 수 있습니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="new 연산자로 함수를 호출할 때 this는 무엇을 가리키나요?"
    choices={[
      "전역 객체",
      "새로 생성되는 인스턴스 객체",
      "함수 자신",
      "undefined",
    ]}
    answer={1}
    explanation="new 연산자로 생성자 함수를 호출하면 새 객체가 생성되고, this가 그 새 객체를 가리킵니다. 생성자 함수 내부에서 this.name = name처럼 인스턴스 프로퍼티를 설정하는 이유입니다."
  />
  <ArticleQuizItem
    mode="multiple"
    question="콜백 함수 내부에서 this를 잃지 않는 방법이 아닌 것은 무엇인가요?"
    choices={[
      "화살표 함수 사용",
      "bind(this) 사용",
      "const self = this로 저장 후 사용",
      "function 키워드를 중첩해서 사용",
    ]}
    answer={3}
    explanation="function 키워드를 중첩해서 사용하면 내부 함수에서 this가 다시 전역 객체로 바뀝니다. 화살표 함수(렉시컬 this), bind, 또는 self = this 패턴이 올바른 해결 방법입니다."
  />
</ArticleQuiz>

---

<Subtitle level={2}>1. this — 상황에 따라 달라지는 참조</Subtitle>

<Paragraph>
  JS의 `this`는 <strong>함수가 호출될 때 동적으로 결정</strong>됩니다. 선언
  위치가 아니라
  <strong>호출 방식</strong>에 따라 다릅니다. (화살표 함수만 예외)
</Paragraph>

<Subtitle level={3}>네 가지 this 바인딩 규칙</Subtitle>

```js
// 1. 일반 함수 호출 — 전역 객체 (strict mode에서는 undefined)
function foo() {
  console.log(this); // window (브라우저)
}
foo();

// 2. 메서드 호출 — 호출한 객체
const obj = {
  name: "철수",
  greet() {
    console.log(this.name); // "철수"
  },
};
obj.greet();

// 3. 생성자 호출 — 새로 생성되는 인스턴스
function Person(name) {
  this.name = name; // 새 인스턴스를 가리킴
}
const p = new Person("영희");

// 4. 명시적 바인딩 — call / apply / bind
function greet() {
  console.log(this.name);
}
greet.call({ name: "민수" }); // "민수"
greet.apply({ name: "지수" }); // "지수"
const boundGreet = greet.bind({ name: "하준" });
boundGreet(); // "하준"
```

<Subtitle level={3}>화살표 함수의 this — 렉시컬 this</Subtitle>

```js
const timer = {
  name: "타이머",
  start() {
    // 일반 함수 — this가 전역으로 바뀜
    setTimeout(function () {
      console.log(this.name); // undefined
    }, 1000);

    // 화살표 함수 — 상위 스코프(start 메서드)의 this 유지
    setTimeout(() => {
      console.log(this.name); // "타이머"
    }, 1000);
  },
};
timer.start();
```

<Paragraph>
  React에서 이벤트 핸들러를 화살표 함수로 쓰는 이유도 이 때문입니다. 클래스
  컴포넌트에서 `this.handleClick = this.handleClick.bind(this)`처럼 bind를 쓰는
  것도 같은 문제를 해결하는 방법입니다.
</Paragraph>

---

<Subtitle level={2}>2. 실행 컨텍스트 — 코드가 실행되는 환경</Subtitle>

<Paragraph>
  실행 컨텍스트는 코드가 실행될 때 JS 엔진이 생성하는{" "}
  <strong>환경 정보 묶음</strong>입니다. 스코프, 호이스팅, 클로저, this가 모두
  실행 컨텍스트 안에서 관리됩니다.
</Paragraph>

```js
const x = "전역";

function outer() {
  const y = "outer";

  function inner() {
    const z = "inner";
    console.log(x, y, z); // 스코프 체인으로 탐색
  }

  inner();
}

outer();

// 콜 스택의 변화:
// 1. 전역 실행 컨텍스트 생성 (x 등록)
// 2. outer() 호출 → outer 실행 컨텍스트 생성 (y 등록)
// 3. inner() 호출 → inner 실행 컨텍스트 생성 (z 등록)
// 4. inner 종료 → inner 컨텍스트 제거
// 5. outer 종료 → outer 컨텍스트 제거
```

<Paragraph>
  각 실행 컨텍스트는 <strong>외부 렉시컬 환경에 대한 참조</strong>를 가지며,
  이것이 스코프 체인과 클로저의 실제 구현 방식입니다.
</Paragraph>

---

<Subtitle level={2}>3. 이터러블과 Set / Map</Subtitle>

<Subtitle level={3}>이터러블이란</Subtitle>

<Paragraph>
  `Symbol.iterator` 메서드를 가진 객체는 이터러블입니다. `for...of`,
  스프레드(`...`), 구조 분해 할당이 내부적으로 이터러블 프로토콜을 사용합니다.
</Paragraph>

```js
// 배열, 문자열, Map, Set — 모두 이터러블
for (const char of "hello") {
  console.log(char); // h, e, l, l, o
}

const [a, b, ...rest] = new Set([1, 2, 3, 4]);
// a=1, b=2, rest=[3, 4]
```

<Subtitle level={3}>Set — 중복 없는 집합</Subtitle>

```js
// 중복 제거
const arr = [1, 2, 2, 3, 3, 4];
const unique = [...new Set(arr)]; // [1, 2, 3, 4]

// 기본 사용
const set = new Set([1, 2, 3]);
set.add(4);
set.has(2); // true
set.delete(1);
set.size; // 3

// 집합 연산
const a = new Set([1, 2, 3]);
const b = new Set([2, 3, 4]);
const union = new Set([...a, ...b]); // {1, 2, 3, 4}
const intersection = new Set([...a].filter((x) => b.has(x))); // {2, 3}
```

<Subtitle level={3}>Map — 키-값 저장소</Subtitle>

```js
// 일반 객체와 달리 어떤 타입이든 키로 사용 가능
const map = new Map();
map.set("name", "철수");
map.set(1, "숫자 키");
map.set({ id: 1 }, "객체 키");

map.get("name"); // "철수"
map.has("name"); // true
map.size; // 3

// 이터러블이라 for...of 가능
for (const [key, value] of map) {
  console.log(key, value);
}

// 객체 → Map 변환
const obj = { a: 1, b: 2 };
const mapFromObj = new Map(Object.entries(obj));
```

---

<Subtitle level={2}>📝 학습 이해 퀴즈</Subtitle>

<Paragraph>
  아래 코드를 <strong>브라우저 콘솔에서 직접 실행</strong>해보고 결과를
  입력해보세요.
</Paragraph>

<ArticleQuiz>
  <ArticleQuizItem
    mode="essay"
    question="아래 코드에서 두 console.log의 출력 결과를 순서대로 입력하세요."
    code={`const obj = {
  value: 10,
  double: function () {
    return this.value * 2;
  },
  doubleArrow: () => {
    return this.value * 2;
  },
};

console.log(obj.double());
console.log(obj.doubleArrow());`}
answer="20, NaN"
explanation="double()은 메서드 호출이므로 this가 obj를 가리켜 10*2=20입니다. doubleArrow는 화살표 함수라 this가 선언 시점의 상위 스코프(전역)를 가리키며, 전역에는 value가 없어 undefined*2 = NaN이 됩니다."
/>

  <ArticleQuizItem
    mode="essay"
    question="아래 코드의 출력 결과는 무엇인가요?"
    code={`const nums = [1, 2, 3, 2, 1, 4];
const result = [...new Set(nums)].length;
console.log(result);`}
    answer="4"
    explanation="Set은 중복을 제거하므로 new Set(nums)는 {1, 2, 3, 4}입니다. 스프레드로 배열로 변환하면 [1, 2, 3, 4]이고 length는 4입니다."
  />
  <ArticleQuizItem
    mode="essay"
    question="아래 코드에서 출력되는 값들을 순서대로 입력하세요."
    code={`const map = new Map([
  ["a", 1],
  ["b", 2],
  ["c", 3],
]);

for (const [key, value] of map) {
if (value > 1) console.log(key);
}`}
answer="b, c"
explanation="Map을 for...of로 순회하면 [key, value] 형태로 구조 분해됩니다. value > 1인 항목은 ['b', 2]와 ['c', 3]이므로 b와 c가 순서대로 출력됩니다."
/>

</ArticleQuiz>

---

<Subtitle level={2}>언제 이 내용이 필요한가?</Subtitle>

<Paragraph>다음 상황에서 이 자료로 돌아오세요.</Paragraph>

- <strong>this 관련</strong>: React 클래스 컴포넌트를 읽거나, 이벤트 핸들러에서
  this가 예상과 다르게 동작할 때
- <strong>실행 컨텍스트</strong>: 면접에서 "호이스팅과 클로저의 원리를
  설명하라"는 질문을 받았을 때
- <strong>Set/Map</strong>: 중복 제거, 빠른 조회가 필요한 데이터 구조가 필요할
  때 (지금 바로 써도 됩니다!)
- <strong>이터러블</strong>: 커스텀 이터레이터를 만들거나, for...of 동작 원리가
  궁금할 때
